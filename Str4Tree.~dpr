program Tree;

uses
  Forms,
  UFrm in 'UFrm.pas' {MainFrm},
  UNode in 'UNode.pas',
  UTree in 'UTree.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TmainFrm, mainFrm);
  Application.Run;
end.
 